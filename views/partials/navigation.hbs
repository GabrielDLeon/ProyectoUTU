{{#if user}}
<nav class="navbar navbar-expand-md navbar-dark bg-dark">
{{else}}
<nav class="navbar navbar-expand-md navbar-light bg-light">
{{/if}}
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            {{#if user }}
                <img src="/img/brand/Klouts_light.png" width="120"></a>
            {{else}}
                <img src="/img/brand/Klouts_dark.png" width="120">
            {{/if}}
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar">
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbar">
            <ul class="navbar-nav ms-auto mb-lg-0">
                {{#if user }}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false"><span class="me-1">{{user.nombre}}</span></a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li class="nav-item"><a class="dropdown-item" href="/profile">Perfil</a></li>
                            <li class="nav-item"><a class="dropdown-item" href="/profile/edit/{{user.id}}">Editar perfil</a></li>
                            <li class="nav-item"><a class="dropdown-item" href="/logout">Cerrar sesión</a></li>
                        </ul>
                    </li>
                {{else}}
                    <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
                    <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
                {{/if}}
                {{#ifCond user.tipo '==' 'empresa'}}
                    <li class="nav-item"><a class="nav-link" href="/create">Hacer publicacion</a></li>
                {{/ifCond}}
            </ul>

            {{!-- <form class="d-flex input-group w-auto">
                <input type="search" class="form-control" placeholder="Type query" aria-label="Search"/>
                <button class="btn btn-outline-primary" type="button" data-mdb-ripple-color="dark">Search</button>
            </form> --}}
            
        </div>
    </div>
</nav>