{{>navigation}}
<div class="container">
    <div class="table-responsive table-lg mt-3">
        <table class="table table-striped table-bordered table-hover border-3 text-center">
            <thead>
                <tr>
                    <th>Publicación</th>
                    <th>Precio</th>
                    <th  class="max-width">Descripción</th>
                    <th>Comentarios</th>
                    <th>Acción</th>
                </tr>
            </thead>
            <tbody>
                {{#if publicacion}}
                    {{#each publicacion}}
                    <tr>
                        <td class="text-nowrap align-middle"><span>{{titulo}}</span></td>
                        <td class="text-nowrap align-middle"><span>{{precio}}</span></td>
                        <td class="text-center align-middle"><span>{{descripcion}}</span></td>
                        <td class="text-center align-middle"><span>{{cantPreguntas}}</span></td>
                        <td class="text-center align-middle">
                            <div class="btn-group align-top m-2">
                                <form action="/list/edit/{{nroPublicacion}}" method="GET">
                                    <button style="margin-right: 10px;" title="Editar" class="btn btn-outline"> <i class="fas fa-edit"></i> </button>
                                </form>
                                <form action="/list/delete/{{nroPublicacion}}" method="POST">
                                    <button style="margin-right: 10px;" title="Eliminar" class="btn btn-outline"> <i class="fas fa-trash"></i> </button>
                                </form>
                                <a title="Ver" href="/publication/{{nroPublicacion}}" class="btn btn-outline link-unstyled"> <i class="fas fa-eye"></i> </a>
                            </div>
                        </td>
                    </tr>
                    {{/each}}
                {{/if}}
            </tbody>
        </table>
    </div>
</div>