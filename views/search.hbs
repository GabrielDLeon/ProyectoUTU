{{>navigation}}

<body>
  {{>navCategories}}
  <div class="container mt-5 p-5">
    <div class="row p-5 top">
      {{#if shops}}
      <h1>Tiendas</h1>
      <div class="row row-cols-lg-4 row-cols-sm-3 g-4 mb-3">
        {{>shops}}
      </div>
      {{/if}}
      {{#if recommendations}}
      <h1>Productos</h1>
      <div class="row row-cols-lg-4 row-cols-sm-2 g-4">
        {{#each recommendations}}
        {{>products}}
        {{/each}}
      </div>
      {{else}}
      <div class="d-flex justify-content-center">
        <div class="alert alert-danger" role="alert" style="width: 70%;">
          <p class="text-danger text-center mt-1 fw-bold">No hay coincidencias con la búsqueda</p>
          <i class="fas fa-exclamation-triangle d-flex justify-content-center" style="font-size: 30px;"></i>
        </div>
      </div>
      {{/if}}
      {{>pagination}}
    </div>
  </div>
  </div>
</body>